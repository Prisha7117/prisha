#include<stdio.h>
main()
{

int w,l;
printf("Enter the width of rectangle (in cm) :");
scanf("%d",&w);
printf("Enter the length of rectangle (in cm) :");
scanf("%d",&l);

int area=w*l;
printf("Area of rectangle will be : %d cm",area);

}
